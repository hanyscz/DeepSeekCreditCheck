<Window x:Class="DeepSeekCreditCheck.UI.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Přihlášení k DeepSeek Platformě"
        Width="800" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A1A"
        Foreground="#E0E0E0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top status message -->
        <Border Grid.Row="0" Background="#252525" BorderBrush="#3A3A3A" BorderThickness="0,0,0,1" Padding="12">
            <TextBlock HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" FontSize="13" Foreground="#B0BEC5">
                Přihlaste se prosím ke svému účtu DeepSeek. Jakmile se přihlásíte, aplikace automaticky zachytí váš session token a okno se samo zavře.
            </TextBlock>
        </Border>

        <!-- WebView2 element -->
        <wv2:WebView2 x:Name="webView" Grid.Row="1" Source="https://platform.deepseek.com/" />
    </Grid>
</Window>
