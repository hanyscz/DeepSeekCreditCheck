<Window x:Class="DeepSeekCreditCheck.UI.Windows.ViewDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📁 Historie zůstatků — DeepSeek Credit Checker"
        Width="700" Height="500"
        MinWidth="500" MinHeight="350"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A1A"
        Foreground="#E0E0E0">
    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="AlternatingRowBackground" Value="#252525" />
            <Setter Property="RowBackground" Value="#1E1E1E" />
            <Setter Property="BorderBrush" Value="#3A3A3A" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserSortColumns" Value="True" />
            <Setter Property="CanUserResizeColumns" Value="True" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2A2A2A" />
                        <Setter Property="Foreground" Value="#AAA" />
                        <Setter Property="BorderBrush" Value="#3A3A3A" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Padding" Value="8,6" />
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="#E0E0E0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="8,4" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="#1E1E1E" />
                        <Setter Property="Foreground" Value="#E0E0E0" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2E7D32" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="BorderBrush" Value="#4CAF50" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333333" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#252525" CornerRadius="6" Padding="14" Margin="0,0,0,10"
                BorderBrush="#3A3A3A" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="📁 Historie zůstatků" FontSize="16" FontWeight="SemiBold" Foreground="#E0E0E0" />
                    <TextBlock Text="{Binding RecordCount, StringFormat='{}{0} záznamů'}"
                               FontSize="12" Foreground="#888" Margin="0,2,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="📥 Export CSV" Click="ExportCsv_Click"
                        Background="#1B5E20" Foreground="White"
                        BorderThickness="0" Padding="12,6" Cursor="Hand"
                        FontSize="13" Margin="0,0,8,0" />
                <Button Grid.Column="3" Content="🗑 Smazat vybrané" Click="DeleteSelected_Click"
                        Background="#C62828" Foreground="White"
                        BorderThickness="0" Padding="12,6" Cursor="Hand"
                        FontSize="13" />
                <Button Grid.Column="5" Content="✕ Zavřít" Click="Close_Click"
                        Background="#3A3A3A" Foreground="#E0E0E0"
                        BorderThickness="0" Padding="12,6" Cursor="Hand"
                        FontSize="13" />
            </Grid>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" x:Name="RecordsGrid" ItemsSource="{Binding Records}"
                  IsReadOnly="True" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Čas" Binding="{Binding Timestamp, StringFormat=\{0:dd.MM.yyyy HH:mm\}}"
                                    Width="150" SortDirection="Descending" />
                <DataGridTextColumn Header="Měna" Binding="{Binding Currency}"
                                    Width="50" />
                <DataGridTextColumn Header="Zůstatek" Binding="{Binding TotalBalanceDecimal, StringFormat=\{0:F2\}}"
                                    Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer info -->
        <TextBlock Grid.Row="2" TextAlignment="Center" FontSize="11" Foreground="#666">
            Podrž Ctrl pro výběr více záznamů
        </TextBlock>
    </Grid>
</Window>
